#ifndef HELPERS_H
#define HELPERS_H

#include "mylibs.h"
#include "consts.h"
bool again(std::string question);
void printRules();
void clear();
std::vector<std::vector<char>> pointerToVector(char** board);

#endif