#include <iostream>
#include <cmath>

int main() {
    // Find more att https://www.cplusplus.com/reference/cmath/

    double x = 3.14;
    double y = 4.99;
    double z;

    //z = std::max(x, y);
    //z = std::min(x, y);
    //z = pow(2, 3);
    //z = sqrt(9);
    //z = abs(-3);
    //z = round(x);
    //z = ceil(x);
    z = floor(y);

    std::cout << z;

    return 0;
}