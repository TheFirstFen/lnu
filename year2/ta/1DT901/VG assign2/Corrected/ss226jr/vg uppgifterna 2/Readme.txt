Iterations:
birthday candles: 
A function definition for the whole code with no return(not needed), starting with base
number 0 for the remaining candles and total number of boxes then formulated the outcome 
printed everything.

countdigits:
An input and then a definition to decide wether the number written is even, odd, or a zero.
Print the results for each of them.

Lists:
salary revision:
An input then used function split for the reason that multiple numbers will be written
afterwards 3 different definitions were written for median, average and gap. All definitions
should formulate and compute the sensible outcome.